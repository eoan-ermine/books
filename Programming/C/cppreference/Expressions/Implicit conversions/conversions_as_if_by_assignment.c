// * In the assignment operator, the value of the right-hand
// operand is converted to the unqualified type of the left-
// hand operand

// * In scalar initialization, the value of the initializer
// expression is converted to the unqualified type of the
// object being initialized

// * In a function-call expression, to a function that has
// a prototype, the value of each argument expression is
// converted to the type of the unqualified declared types
// of the corresponding parameter

// * In a return statement, the value of the operand of return
// is converted to an object having the return type of the
// function